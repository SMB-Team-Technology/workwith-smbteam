name: Audit Notify (Email only)

on:
  push:
    branches: [main]
    paths:
      - 'triggers/audit-notify/*.json'

jobs:
  send-email:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: main

      - name: Find trigger file and parse inputs
        id: inputs
        run: |
          TRIGGER_FILE=$(git diff-tree --no-commit-id -r --name-only "${{ github.sha }}" \
            | grep '^triggers/audit-notify/.*\.json$' | head -1)
          echo "firm_name=$(jq -r '.firm_name' "$TRIGGER_FILE")"         >> "$GITHUB_OUTPUT"
          echo "friendly_name=$(jq -r '.friendly_name' "$TRIGGER_FILE")" >> "$GITHUB_OUTPUT"
          echo "sales_rep=$(jq -r '.sales_rep' "$TRIGGER_FILE")"         >> "$GITHUB_OUTPUT"
          echo "test_only=$(jq -r '.test_only // "true"' "$TRIGGER_FILE")" >> "$GITHUB_OUTPUT"
          echo "closer_email=$(jq -r '.closer_email // ""' "$TRIGGER_FILE")" >> "$GITHUB_OUTPUT"

      - name: Send email with PDF + PPTX attachments
        env:
          GMAIL_USER: ${{ secrets.GMAIL_USER }}
          GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
          FIRM_NAME: ${{ steps.inputs.outputs.firm_name }}
          FRIENDLY_NAME: ${{ steps.inputs.outputs.friendly_name }}
          SALES_REP: ${{ steps.inputs.outputs.sales_rep }}
          TEST_ONLY: ${{ steps.inputs.outputs.test_only }}
          CLOSER_EMAIL: ${{ steps.inputs.outputs.closer_email }}
        run: |
          python3 - << 'PYEOF'
          import smtplib, os, glob, sys
          from email.mime.multipart import MIMEMultipart
          from email.mime.text import MIMEText
          from email.mime.application import MIMEApplication

          gmail_user     = os.environ["GMAIL_USER"]
          gmail_password = os.environ["GMAIL_APP_PASSWORD"]
          firm_name      = os.environ["FIRM_NAME"]
          friendly_name  = os.environ["FRIENDLY_NAME"]
          sales_rep      = os.environ["SALES_REP"]
          test_only      = os.environ.get("TEST_ONLY", "true").lower() == "true"
          closer_email   = os.environ.get("CLOSER_EMAIL", "")
          eli            = "eli.gill@smbteam.com"

          if test_only:
              to_addr, cc_addrs = eli, []
              print(f"TEST MODE: sending only to {eli}")
          else:
              if not closer_email:
                  print("ERROR: closer_email not set in trigger file.")
                  sys.exit(1)
              to_addr, cc_addrs = closer_email, [eli]

          pdf_files  = glob.glob(f"{friendly_name}/*_Sales_Companion.pdf")
          pptx_files = glob.glob(f"{friendly_name}/*_Proposal.pptx")
          attachments = pdf_files + pptx_files

          if not attachments:
              print(f"ERROR: No PDF or PPTX found in {friendly_name}/")
              sys.exit(1)

          msg = MIMEMultipart()
          msg["From"]    = gmail_user
          msg["To"]      = to_addr
          if cc_addrs:
              msg["Cc"]  = ", ".join(cc_addrs)
          msg["Subject"] = f"Growth, Profit, and Freedom Roadmap ready for {firm_name}"

          body = "\n".join([
              f"Hi {sales_rep},",
              "",
              f"The Growth, Profit, and Freedom Roadmap for {firm_name} is ready ahead of your proposal call.",
              "",
              "Attached you'll find:",
              "  - Proposal Deck (PowerPoint) — share this on screen during the call",
              "  - Sales Companion (PDF) — your seller-only cheat sheet, not for the client",
              "",
              "Let the team know if you need any changes before the call.",
              "",
              "SMB Team",
              "",
          ])
          msg.attach(MIMEText(body, "plain"))

          for filepath in attachments:
              with open(filepath, "rb") as f:
                  part = MIMEApplication(f.read(), Name=os.path.basename(filepath))
              part["Content-Disposition"] = f'attachment; filename="{os.path.basename(filepath)}"'
              msg.attach(part)
              print(f"Attached: {filepath}")

          with smtplib.SMTP("smtp.gmail.com", 587) as server:
              server.starttls()
              server.login(gmail_user, gmail_password)
              server.sendmail(gmail_user, [to_addr] + cc_addrs, msg.as_string())

          print(f"Sent to {to_addr}" + (f", CC: {', '.join(cc_addrs)}" if cc_addrs else ""))
          PYEOF
